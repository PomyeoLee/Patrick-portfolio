# How to run this snapshot

```bash
cd development-journey/block-03-polish-deploy
npm install
npm run dev
# → http://localhost:3000
```

See `WORK_LOG.md` for the human work record and `VERSION.md` for the tag.
